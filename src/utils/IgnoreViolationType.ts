/**
 * This is a type to recognize the type of action
 * to do to ignore a violation and defines its
 * scope (either project or file).
 */
export enum IgnoreViolationType {
  File = 1,
  Project = 2,
}
